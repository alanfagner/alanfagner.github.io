import styled from "styled-components";

import { MainContainer } from "../../rules/style";

const ExpProfissionalBox = styled(MainContainer)`
  .card-skill {
    padding: 1em;
  }
`;
export default ExpProfissionalBox;
